{
  "task_type": "raid",
  "task_config": {
    "turn": 1,
    "refresh": true,
    "until_finish": false,
    "trigger_skip": false,
    "pre_fa": false,
    "min_hp_threshold": 60,
    "max_hp_threshold": 100,
    "min_people": 1,
    "max_people": 12,
    "summon_priority": [
      {
        "level": "Lvl 250",
        "name": "Titan"
      },
      {
        "level": "Lvl 100",
        "name": "Huanglong"
      },
      {
        "level": "Lvl 150",
        "name": "Kaguya"
      }
    ]
  },
  "actions": [
    {
      "name": "refresh_raid_list",
      "params": {},
      "transitions": {
        "failed": "refresh_raid_list",
        "success": "select_raid"
      }
    },
    {
      "name": "select_raid",
      "params": {
        "filter": "prefer_Lv100"
      },
      "transitions": {
        "failed": "refresh_raid_list",
        "success": "select_summon"
      }
    },
    {
      "name": "select_summon",
      "params": {},
      "transitions": {
        "failed": "select_raid",
        "success": "join_battle"
      }
    },
    {
      "name": "join_battle",
      "params": {},
      "transitions": {
        "failed": "select_raid",
        "success": "do_battle"
      }
    },
    {
      "name": "do_battle",
      "params": {
        "mode": "full_auto"
      },
      "transitions": {
        "failed": "select_raid",
        "success": "select_raid"
      }
    }
  ],
  "exit_condition": {
    "type": "raid_count",
    "value": 10
  }
}